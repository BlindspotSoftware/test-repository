# Example Test Configuration for AMI-RPL-SOMETHING
name: Example Test
description: This is a sample test configuration

binary-not-required: true

stages:
- name: Example Stage
  steps:
  - cmd: cmd
    name: Example Step
    transport:
      proto: local
    parameters:
      executable: echo
      args: ["Hello, World!"]
